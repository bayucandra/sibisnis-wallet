import ActionTypes from '../action-types';

import { ofType } from 'redux-observable';
/*
const sseLogout = action$ => {
  return action$.pipe(
    ofType( ActionTypes.app.LOGOUT )
  );
};

const appEpics = [ sseLogout ];

export default appEpics;*/